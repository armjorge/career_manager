# Minimal dependencies for AWS Lambda (zip deploy).
# Build: pip install -r backend/requirements-lambda.txt -t package/
#        cp -r backend package/ && cd package && zip -r ../lambda.zip .

fastapi>=0.115.0
httpx>=0.28.0
mangum>=0.19.0
psycopg2-binary>=2.9.11
pydantic-settings>=2.6.0
pyjwt[crypto]>=2.10.0
